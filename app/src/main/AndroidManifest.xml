<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loditech.bitloanmerchant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC"/>
    <uses-permission android:name="android.hardware.nfc" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeMenuActivity" />
        <activity android:name=".NFC_Activity" />
        <activity android:name=".SettingsA_Activity" />
        <activity android:name=".SettingsB_Activity" />
        <activity android:name=".SettingsC_Activity" />
        <activity android:name=".SettingsD_Activity" />
        <activity android:name=".SettingsMenuActivity" />
        <activity android:name=".SignupA_Activity" />
        <activity android:name=".SignupB_Activity" />
        <activity android:name=".TransactionsActivity" />
    </application>

</manifest>